apiVersion: v1
kind: Service
metadata:
  name: {{ include "posting-manager.fullname" . }}
  labels: {{- include "posting-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "posting-manager.selectorLabels" . | nindent 4 }}
